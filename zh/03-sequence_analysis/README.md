# 模块 3：序列分析

## 引言

在模块 1 中，你学会了在 NCBI 等公共数据库中搜索和下载序列，并区分 FASTA 和 GenBank 等格式。在模块 2 中，你获得了从终端工作和用 Python 编程的工具。现在，在本模块中，你将使用所有这些来做生物信息学家大部分时间在做的事情：**比较序列**。

比较序列可能是生物信息学中最基本的操作。你刚获得的那条序列与已知的什么东西相似吗？有多相似？在哪些位置不同？在哪里设计引物来扩增它？所有这些问题都通过比较序列来回答，在本模块中你将在概念、算法和实践层面学习如何做到这一点。

我们将围绕三个主要部分展开：

1. **序列比对（Sequence Alignment）**，理解允许精确比较两条序列的动态规划算法。
2. **BLAST**，世界上使用最广泛的在数据库中搜索相似序列的工具——现在你知道了什么是比对，你将理解它是如何工作以及为什么能工作。
3. **引物设计和*in silico* PCR**，其中序列比较应用于具体的实验问题。

> [!NOTE]
> 知道如何比较序列不仅仅是一项技术技能：它是系统发育学、比较基因组学、变异检测以及几乎你职业生涯中会做的任何生物信息学分析所建立的基础。

---

## 1. 序列比对

### 1.1 什么是比对以及为什么重要？

**序列比对（sequence alignment）**将两条（或更多）序列上下排列，以识别等价位置：匹配、差异、插入和缺失。

从生物学角度来看，比对序列允许你：

- 鉴定**同源基因**（homologous genes，共享共同祖先的基因）；
- 检测变异体之间的**突变**；
- 找到可能具有重要功能的**保守区域**；
- 评估生物之间的**进化距离**。

### 1.2 点阵图（Dot Plot）

在讨论复杂算法之前，有一种令人惊讶的简单且强大的可视化工具：**点阵图**或 *dot plot*。

将两条序列放在一个表格的轴上（一条在水平轴，另一条在垂直轴）。在碱基匹配的每个单元格中标记一个点。完成后，出现的图案揭示了这两条序列之间的关系，**无需进行正式的比对**。

#### 可以检测到哪些图案？

| Dot plot 上的图案 | 表示什么 |
|---|---|
| **连续对角线** | 两条序列之间的高度相似性区域 |
| **位移对角线**（平行于主对角线） | 重复序列或复制 |
| **垂直对角线**（反对角线） | 反向互补序列（回文或反向重复） |
| **对角线断裂** | 插入、缺失或分化区域 |
| **多条短对角线** | 分散重复或低复杂度 |
| **无明显图案** | 序列不共享显著相似性 |

> [!TIP]
> Dot plot 不能告诉你*哪个*是最佳比对，但它给出了两条序列之间关系的**全局视图**，这是任何单独比对都无法提供的。就像在走进街道之前先看地图。

工具：**Dotlet**（web）、**Gepard**（Java）、**D-Genies**（web）

### 1.3 全局比对 vs. 局部比对

这是一个根本性的区别：

#### 全局比对（Global Alignment）

**从头到尾**比较序列，尝试对齐其整个长度。

> [!TIP]
> **何时使用？** 当两条序列是同一基因或区域且长度相似时。

#### 局部比对（Local Alignment）

仅在长度可能非常不同的序列内搜索**最佳相似性区域**。

> [!TIP]
> **何时使用？** 当只有序列的一部分共享相似性时（例如，长蛋白质内的一个结构域，或基因组中的一个保守区域）。

简而言之：
- **全局** = "让我们从头到尾比较所有的东西"
- **局部** = "让我们找到最佳共享片段"

> [!NOTE]
> BLAST 使用**局部**比对。这就是它为什么能在非常不同的序列之间找到部分相似性。

### 1.4 打分（Scoring）概念

为了判断一个比对是"好"还是"坏"，我们需要一个打分系统（*scoring*）。

#### Match 和 Mismatch

对于 DNA 序列，最简单的做法是分配：
- 每个匹配（*match*）的**奖励**，例如 **+1**；
- 每个差异（*mismatch*）的**罚分**，例如 **-1**。

#### Gap 罚分

**Gap**（`-`）代表插入或缺失（*indel*）。如果我们不惩罚 gap，算法可能会引入不必要的 gap 以最大化匹配。

两种主要模型：
- **线性罚分：** 每个 gap 成本相同（例如，每个 gap **-2**）。
- **仿射罚分（affine penalty）：** 打开 gap 的成本高于扩展它。这更符合生物学实际。

#### 替代矩阵（Substitution Matrices，用于蛋白质）

在蛋白质中，并非所有替代都一样。将亮氨酸变为异亮氨酸（二者都是疏水性氨基酸）比将亮氨酸变为天冬氨酸（疏水性→带电）不那么"严重"。**BLOSUM** 和 **PAM** 矩阵通过为每对氨基酸分配不同分数来捕捉这些差异。

对于本模块中的 DNA，我们将使用简单的 match/mismatch/gap 方案。

### 1.5 Needleman-Wunsch 算法（全局比对）

该算法于 1970 年发表，是第一个使用**动态规划（dynamic programming）**精确解决全局比对问题的算法。

#### 示例：逐步演示

比对序列 `GCATG` 和 `GATTG`，使用：
- Match: **+1**
- Mismatch: **-1**
- Gap: **-2**

**步骤 1：创建矩阵**

```text
        -     G     C     A     T     G
  -     0    -2    -4    -6    -8   -10
  G    -2
  A    -4
  T    -6
  T    -8
  G   -10
```

**步骤 2：填充矩阵**

对于每个单元格，计算三个选项中的最大值：

1. **对角线**（来自左上）：对角线单元格 + match/mismatch。
2. **上方**：上方单元格 + gap。
3. **左侧**：左侧单元格 + gap。

完整矩阵：

```text
        -     G     C     A     T     G
  -     0    -2    -4    -6    -8   -10
  G    -2     1    -1    -3    -5    -7
  A    -4    -1     0     0    -2    -4
  T    -6    -3    -2    -1     1    -1
  T    -8    -5    -4    -3     0     0
  G   -10    -7    -6    -5    -2     1
```

**步骤 3：回溯（Traceback）— 重建比对**

```text
Seq 1 (GCATG):   G  C  A  T  G
Seq 2 (GATTG):   G  A  T  T  G
                 *  ·  ·  *  *

分数：+1 -1 -1 +1 +1 = 1  ✓
```

> [!IMPORTANT]
> 重要的不是记住数字，而是理解逻辑：每个单元格代表**到该点为止对齐这两条子序列的最佳方式**，回溯则逐步重建该最优解。

### 1.6 Smith-Waterman 算法（局部比对）

1981 年发表，它是 Needleman-Wunsch 的修改版，有两个关键区别：

| 特征 | Needleman-Wunsch（全局） | Smith-Waterman（局部） |
|---|---|---|
| **初始化** | 第一行和列使用累积罚分 | 第一行和列使用**零** |
| **负值** | 允许 | 替换为 **0**（永不低于零） |
| **回溯** | 从**右下角**单元格开始 | 从矩阵中**值最高**的单元格开始 |
| **回溯结束** | 到达单元格 [0,0] | 到达一个 **0** 时停止 |

**思维模型：** 两种算法都处理完整的序列，但 Needleman-Wunsch 强制从头到尾对齐所有内容，而 Smith-Waterman 仅提取最佳共享片段。

### 1.7 同一性（Identity）vs. 相似性（Similarity）

- **同一性（Identity）：** 比对中**完全相同**位置的百分比。
- **相似性（Similarity）：** 也考虑**保守替代（conservative substitutions）**的相似程度，在蛋白质中特别重要。

在 **DNA** 中，通常讨论**同一性**，因为碱基之间没有可比的化学性质。在**蛋白质**中，**相似性**可能比同一性信息量更大。

---

## 2. 代码示例：Python 中的比对

目标：让你理解比对算法"从内部"如何工作，而不是将其视为黑箱。

脚本位于：`exercises/code/alignment_dp.py`

### 脚本做什么？

1. **`needleman_wunsch(seq1, seq2, match, mismatch, gap)`** — 实现全局比对
2. **`smith_waterman(seq1, seq2, match, mismatch, gap)`** — 实现局部比对
3. **`print_matrix(matrix, seq1, seq2)`** — 以可读格式打印矩阵
4. **主程序块** — 用示例序列运行两种算法并显示结果

### 如何运行

```bash
cd exercises/code/
python3 alignment_dp.py
```

---

## 3. 使用 BLAST 进行相似性搜索

### 3.1 什么是 BLAST？

**BLAST**（*Basic Local Alignment Search Tool*）是生物信息学中用于在数据库中搜索相似序列的最广泛使用的工具。它于 1990 年发表，已成为生物信息学领域相当于网络搜索引擎的工具。

> [!NOTE]
> **类比：** 想象你有一句用未知语言写的短语，你想在一个拥有数百万本书的图书馆中找到相似的短语。你不能读完整本书，所以你先搜索匹配的关键词，然后在这些匹配周围扩展比较，最后评估哪些是真正的相似性，哪些是偶然。

### 3.2 BLAST 的概念性工作原理

BLAST 使用三阶段的**启发式策略**：

1. **种子阶段（Seeding）：** 将你的序列分成短片段（"词"），在数据库中搜索精确或近似匹配（**种子**）。
2. **扩展阶段（Extension）：** 从每个种子向两个方向**扩展比对**，计算分数。
3. **统计评估（Statistical Evaluation）：** 评估比对是否**统计上显著**。

> [!IMPORTANT]
> 这种策略使 BLAST 非常快，但不能保证在所有情况下都能找到最优比对。为此，存在像 Smith-Waterman 这样的精确算法。

### 3.3 BLAST 类型

| 程序 | Query | 数据库 | 使用时机 |
|---|---|---|---|
| **blastn** | 核苷酸 | 核苷酸 | 搜索相似基因，通过 16S rRNA 鉴定物种 |
| **blastp** | 蛋白质 | 蛋白质 | 搜索同源蛋白质，注释功能 |
| **blastx** | 核苷酸（翻译） | 蛋白质 | 你有 DNA，想搜索蛋白质数据库 |
| **tblastn** | 蛋白质 | 核苷酸（翻译） | 使用蛋白质 query 在未注释基因组中搜索基因 |

### 3.4 结果解读

| 参数 | 测量什么 | "好"值意味着什么 |
|---|---|---|
| **E-value** | 在同等大小的数据库中偶然发现该比对的概率 | 越低越好。E-value < 1e-5 通常被认为显著 |
| **Score（bit score）** | 比对的归一化分数 | 越高越好 |
| **% Identity** | 比对中相同位置的百分比 | 取决于生物学背景 |
| **Query coverage** | 你的序列有多少百分比被对齐 | 低 coverage 可能表示只有一小部分相似 |

> [!WARNING]
> **常见错误：** 低 E-value 且 coverage 为 10% 可能意味着只有一个小结构域是相似的，而不是整个蛋白质是同源的。始终同时查看这四个参数。

---

## 4. 引物设计和 *in silico* PCR

### 4.1 什么是引物？

**引物（primer）**是一个短的 DNA 寡核苷酸（约 18–25 nt），与模板序列结合并允许在 PCR 中启动复制。引物设计是碱基互补性和比对概念最直接的应用之一。

### 4.2 良好引物的关键参数

| 参数 | 推荐值 | 原因 |
|---|---|---|
| **长度** | 18–25 nt | 特异性和结合效率之间的平衡 |
| **GC 含量** | 40–60% | 引物-模板双链的稳定性 |
| **Tm** | 55–65 °C | 引物对应具有相似的 Tm（差值 ≤ 2 °C） |
| **3' 端** | 避免错配；有时使用 "GC clamp" | 对聚合酶延伸至关重要 |
| **自互补性** | 低 | 避免发夹结构和引物二聚体 |
| **扩增子大小** | 100–500 bp（常规 PCR） | 过长产物更难扩增 |

### 4.3 设计工具

- **Primer-BLAST**：设计引物并针对数据库评估特异性
- **Primer3**：提出具有可配置约束的引物对的经典工具

详细实践见：📄 **[练习：引物设计](exercises/01_primer_design.md)**

---

## 5. 概念总结

在本模块中你学到了：

- **BLAST** 如何使用启发式方法快速搜索相似性；
- **Needleman-Wunsch** 和 **Smith-Waterman** 算法如何精确解决比对问题；
- 全局与局部比对的选择如何取决于生物学问题；
- **引物设计**如何将计算分析与实验室连接起来。

> [!IMPORTANT]
> 序列分析不是孤立的步骤；它是生物信息学阅读生物学的语言。

---

## 模块练习

| 练习 | 描述 |
|---|---|
| [使用动态规划的序列比对](./exercises/code/alignment_dp.py) | 从头实现 Needleman-Wunsch 和 Smith-Waterman 的 Python 脚本 |
| [引物设计和 *in silico* PCR](exercises/01_primer_design.md) | 使用 Primer-BLAST 设计引物、验证扩增子和 *in silico* 电泳 |
